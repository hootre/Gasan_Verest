<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.verest.board.dao.PortDatDaoImpl">

	<insert id="insert" parameterType="PortDat">
		<selectKey keyProperty="no" resultType="Integer" order="BEFORE">
			SELECT port_dat_seq.NEXTVAL no FROM DUAL
		</selectKey>
		INSERT INTO port_dat (dat_no, board_no, writer, content, regdate)
		VALUES (#{dat_no}, #{board_no}, #{writer}, #{content}, SYSDATE);
	</insert>
	
	<resultMap type="PortDat" id="portDatResultMap">
		<id property="dat_no" column="no" />
		<result property="board_no" column="board_no" />
		<result property="writer" column="writer" />
		<result property="content" column="content" />
		<result property="regdate" column="regdate" />
		<result property="userInfo.id" column="id" />
		<result property="userInfo.name" column="name" />
	</resultMap>
	
	<select id="select" parameterType="String" resultMap="portDatResultMap">
		SELECT p.dat_no, p.board_no, p.writer, p.content, p.regdate u.v_id, u.v_name
		FROM userinfo u, port_dat p
		WHERE p.writer = u.v_id AND no = #{no}
	</select>
	
	<select id="selectAll" resultMap="portDatResultMap">
		SELECT p.dat_no, p.board_no, p.writer, p.content, p.regdate u.v_id, u.v_name
		FROM userinfo u, port p
		WHERE p.writer = u.v_id ORDER BY p.no DESC
	</select>
	
	<select id="count" resultType="int">
		SELECT COUNT(*) cnt FROM port_dat WHERE 1=1
	</select>
	
	<update id="update" parameterType="PortDat">
		UPDATE port_dat
		SET content = #{content}, regdate = SYSDATE
		WHERE no = #{no}
	</update>
	
	<delete id="delete" parameterType="String">
		DELETE FROM port_dat WHERE no = #{no}
	</delete>
	
</mapper>