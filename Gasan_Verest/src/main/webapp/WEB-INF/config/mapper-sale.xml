<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.verest.board.dao.ProjectDaoImpl">

	<insert id="insert" parameterType="Project">
		<selectKey keyProperty="no" resultType="Integer" order="BEFORE">
			SELECT sale_seq.NEXTVAL no FROM DUAL
		</selectKey>
		INSERT INTO sale (no, title, content, writer, price, regdate, attachment, views)
		VALUES (#{no}, #{title}, #{content}, #{writer}, #{price}, SYSDATE, #{attachment}, 0)
	</insert>
	
	<resultMap type="Project" id="projectResultMap">
		<id property="no" column="no" />
		<result property="title" column="title" />
		<result property="content" column="content" />
		<result property="writer" column="writer" />
		<result property="price" column="price" />
		<result property="regdate" column="regdate" />
		<result property="attachment" column="attachment" />
		<result property="userInfo.id" column="id" />
		<result property="userInfo.name" column="name" />
	</resultMap>
	
	<select id="select" parameterType="String" resultMap="projectResultMap">
		SELECT p.no, p.title, p.content, p.writer, p.price, p.regdate, p.attachment, u.v_id, u.v_name
		FROM userinfo u, port p
		WHERE p.writer = u.v_id AND no = #{no}
	</select>
	
	<select id="selectAll" resultMap="projectResultMap">
		SELECT p.no, p.title, p.content, p.writer, p.price, p.regdate, p.attachment, u.v_id, u.v_name
		FROM userinfo u, port p
		WHERE p.writer = u.v_id ORDER BY p.no DESC
	</select>
	
	<select id="count" resultType="int">
		SELECT COUNT(*) cnt FROM sale WHERE 1=1
	</select>
	
	<update id="update" parameterType="Project">
		UPDATE sale 
		SET title = #{title}, content = #{content}, price = #{price}, regdate = SYSDATE, attachment = #{attachment}
		WHERE no = #{no}
	</update>
	
	<delete id="delete" parameterType="String">
		DELETE FROM sale WHERE no = #{no}
	</delete>
	
</mapper>